---
description: Guided feature development with codebase understanding and architecture focus. Modes (pick one): --fast (skip test/review), --hard (mandatory reviewers, no auto-approve). Composable flags (any mode): --no-test (skip tester), --tdd (tests-first). Accepts an optional plan file path: /ck:cook plans/260418-auth/plan.md
---

Load the `ck:cook` skill and run it with `$ARGUMENTS`.
