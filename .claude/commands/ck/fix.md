---
description: Structured bug-fix pipeline. Scout → Diagnose+Fix → Review → Finalize. Modes: --fast (skip review + docs), --hard (mandatory review, no auto-approve).
---

Load the `ck:fix` skill and run it with `$ARGUMENTS`.
